~$*
__pycache__/
*.py[cod]
.pytest_cache/
pytest-cache-files-*/
.venv/
# The local development database is backend/pangeaworld.db. Never commit a
# database copied into the repository root or any other generated SQLite file.
*.db
phase2-*.log
endurance-logs/

# Provider credentials are configured only in the backend environment.
.env
.env.*
!.env.example
